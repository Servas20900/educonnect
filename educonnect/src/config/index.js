const defaultConfig = {
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 8,
  miniDrawer: false,
  mode: 'light'
};

// routes
export const DASHBOARD_PATH = '/dashboard';

export default defaultConfig;
